#include "black_state.h"
#include <iostream>

void BlackState::Download(const std::string &username, const std::string &item,
        DownloadManager *manager)
{
    std::cout << "涉嫌恶意下载，禁止登录本系统...." << std::endl;
}